<!--
  This PR must target `dev`, not `main`. `main` is the protected, stable
  branch and only receives merges from `dev` — see CONTRIBUTING.md.

  If the base branch shown above is not `dev`, change it before requesting
  review (use the "Edit" button next to the PR title, or the base branch
  dropdown when opening the PR).
-->

## Summary

<!-- What does this PR change, and why? -->

## Checklist

- [ ] This PR's base branch is `dev` (not `main`)
- [ ] My branch was created from `dev`
